[package]
name = "flshare-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.54", features = ["derive"] }
notify = "8.2.0"
tokio = { version = "1.49.0", features = ["full"] }
watcher = { path = "../../crates/watcher" }
